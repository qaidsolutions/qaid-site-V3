
import Header from '../components/Header'; import Footer from '../components/Footer'; import {useState} from 'react';
export default function Contact(){ const [s,setS]=useState({name:'',email:'',message:'',sent:false}); async function submit(e){ e.preventDefault(); const res=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(s)}); if((await res.json()).ok) setS({...s,sent:true}); }
return (<div><Header/><main className='section'><div className='max-w-3xl mx-auto'><h2 className='text-3xl font-bold'>Get in touch</h2>{s.sent? <div className='text-green-400'>Message sent</div>:<form onSubmit={submit} className='mt-4 flex flex-col gap-3'><input required placeholder='Name' className='p-2' value={s.name} onChange={e=>setS({...s,name:e.target.value})}/><input required placeholder='Email' className='p-2' value={s.email} onChange={e=>setS({...s,email:e.target.value})}/><textarea required placeholder='Message' className='p-2' value={s.message} onChange={e=>setS({...s,message:e.target.value})}/><button className='btn-primary'>Send</button></form>}</div></main><Footer/></div>); }
