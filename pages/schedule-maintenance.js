
import Header from '../components/Header'; import Footer from '../components/Footer'; import {useState} from 'react';
export default function Schedule(){ const [s,setS]=useState({name:'',email:'',serial:'',date:'',sent:false}); async function submit(e){ e.preventDefault(); const res=await fetch('/api/schedule',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(s)}); if(res.ok) setS({...s,sent:true}); }
return (<div><Header/><main className='section'><div className='max-w-2xl mx-auto card'><h2 className='text-2xl font-bold'>Schedule Maintenance</h2>{s.sent? <div className='text-green-400'>Booked</div>:<form onSubmit={submit} className='mt-4 flex flex-col gap-3'><input required placeholder='Name' className='p-2' value={s.name} onChange={e=>setS({...s,name:e.target.value})}/><input required placeholder='Email' className='p-2' value={s.email} onChange={e=>setS({...s,email:e.target.value})}/><input required placeholder='Serial' className='p-2' value={s.serial} onChange={e=>setS({...s,serial:e.target.value})}/><input type='date' required className='p-2' value={s.date} onChange={e=>setS({...s,date:e.target.value})}/><button className='btn-primary'>Book</button></form>}</div></main><Footer/></div>); }
