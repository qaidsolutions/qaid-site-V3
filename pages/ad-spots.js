
import Header from '../components/Header'; import Footer from '../components/Footer'; import {useState} from 'react';
export default function Ads(){ const [s,setS]=useState({company:'',email:'',content:''}); const [msg,setMsg]=useState('');
async function submit(e){ e.preventDefault(); const res=await fetch('/api/ads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(s)}); const j=await res.json(); if(j.ok) setMsg('Submitted'); else setMsg('Rejected: '+(j.reason||'')); }
return (<div><Header/><main className='section'><div className='max-w-3xl mx-auto card'><h2 className='text-2xl font-bold'>Advertise on Q·aid</h2><form onSubmit={submit} className='mt-4 flex flex-col gap-3'><input required placeholder='Company' className='p-2' value={s.company} onChange={e=>setS({...s,company:e.target.value})}/><input required placeholder='Email' className='p-2' value={s.email} onChange={e=>setS({...s,email:e.target.value})}/><textarea required placeholder='Ad content' className='p-2' value={s.content} onChange={e=>setS({...s,content:e.target.value})}/><button className='btn-primary'>Submit</button></form>{msg && <div className='mt-3 text-green-400'>{msg}</div>}</div></main><Footer/></div>); }
